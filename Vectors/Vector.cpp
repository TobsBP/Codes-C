#include <iostream>
using namespace std;

int main()
{

    // Player[10] is how big are the memory so in this vector contain 10 players
    int Player[10];
    int i = 0;

    cout << "Put the number of the player who scored: " << endl;
    cin >> Player[0];

    cin >> Player[i];

    cout << "Goals scored by the player: " << Player[0] << endl;

    return 0;
}